route:
  group_by: ["alertname", "instance"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 2h
  receiver: "default"

receivers:
  - name: "default"
    # ① 邮件示例（需要可用的 SMTP）
    # email_configs:
    #   - to: "you@example.com"
    #     from: "alert@example.com"
    #     smarthost: "smtp.example.com:587"
    #     auth_username: "user"
    #     auth_identity: "user"
    #     auth_password: "pass"

    # ② Webhook 示例（对接自研告警机器人）
    webhook_configs:
      - url: "http://your-webhook-endpoint/alert"
        send_resolved: true
